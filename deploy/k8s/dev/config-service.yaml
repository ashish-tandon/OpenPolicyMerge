apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-service
  namespace: openpolicy-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: config-service
  template:
    metadata:
      labels:
        app: config-service
    spec:
      containers:
      - name: config-service
        image: openpolicy/config-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9005
        env:
        - name: SERVICE_PORT
          value: "9005"
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: openpolicy-config
              key: DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: openpolicy-config
              key: REDIS_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 9005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 9005
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: config-service
  namespace: openpolicy-platform
spec:
  type: ClusterIP
  ports:
  - port: 9005
    targetPort: 9005
    protocol: TCP
    name: http
  selector:
    app: config-service
